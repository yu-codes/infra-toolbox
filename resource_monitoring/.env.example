# ============================================
# Resource Monitoring Service 配置
# ============================================

# --- 內部服務連接 ---
NODE_EXPORTER_URL=http://node_exporter:9100
CADVISOR_URL=http://cadvisor:8080

# --- API 設定 ---
API_HOST=0.0.0.0
API_PORT=8000

# --- CPU 採樣配置 ---
# CPU 使用率計算間隔 (分鐘)
# 服務會每隔此時間採樣一次 CPU 數據，API 回傳的是該區間內的平均使用率
CPU_SAMPLE_INTERVAL_MINUTES=1

# --- 資料儲存 ---
# 採樣資料儲存目錄 (容器內路徑)
DATA_DIR=/app/data
# ============================================
# Log 監控配置（新增）
# ============================================

# --- 日誌路徑監控 ---
# 監控的日誌路徑，多個用分號隔開
# 支持跨平台路徑：
#   Linux/macOS: /var/log/docker;/var/log/app
#   Windows:     C:\logs\docker;C:\logs\app
#   Docker:      /host/logs/docker;/host/logs/app
LOG_MONITOR_PATHS=

# --- Log 監控啟用狀態 ---
# 是否啟用日誌監控 (true/false)
LOG_MONITOR_ENABLED=true

# --- 活躍判定時間閾值 ---
# 檔案在此時間內有修改則視為服務活躍（分鐘）
# 建議值：
#   高頻應用（Web 伺服器）: 1-2 分鐘
#   中頻應用（後台服務）: 5-10 分鐘
#   低頻應用（批處理）: 30-60 分鐘
LOG_ACTIVITY_THRESHOLD_MINUTES=5